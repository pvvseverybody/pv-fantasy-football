'use client';
import {useEffect,useState} from 'react';
import SiteHeader from '../components/SiteHeader';
export default function Leaderboard(){const [data,setData]=useState(null),[tab,setTab]=useState('weekly'),[error,setError]=useState('');useEffect(()=>{fetch('/api/leaderboard').then(async response=>{if(!response.ok)throw new Error();return response.json()}).then(setData).catch(()=>setError('Standings are temporarily unavailable.'));},[]);const rows=tab==='weekly'?data?.weekly:data?.cumulative;return <main className="publicPage"><SiteHeader/><section className="contentShell"><div className="pageIntro"><p className="eyebrow">STANDINGS</p><h1>Leaderboard.</h1><p>Weekly performance and the cumulative season race.</p></div><div className="tabBar"><button className={tab==='weekly'?'active':''} onClick={()=>setTab('weekly')}>{data?.week||'Current week'}</button><button className={tab==='cumulative'?'active':''} onClick={()=>setTab('cumulative')}>Season</button></div>{error&&<div className="stateMessage failure"><strong>Unable to load standings</strong><p>{error}</p></div>}{data&&rows?.length===0&&<div className="emptyState"><h2>Standings are coming</h2><p>No published participant scores are available yet. Check back after game scoring begins.</p></div>}<div className="leaderList">{rows?.map(row=><article key={`${tab}-${row.rank}-${row.participant}`}><b>{row.rank}</b><strong>{row.participant}</strong><div><span>{tab==='weekly'?'PTS':'TOTAL'}</span><em>{(tab==='weekly'?row.points:row.season_points).toFixed(1)}</em></div></article>)}</div><a className="textLink" href="/results">View my player-by-player results →</a></section></main>}
